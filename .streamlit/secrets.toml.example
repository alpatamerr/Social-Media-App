# Streamlit Cloud secrets — add these in the Streamlit Cloud dashboard
# under App Settings → Secrets (do NOT commit a .toml file with real values).
#
# Copy this file's contents into the Streamlit Cloud secrets editor
# and replace the placeholder with your Railway backend URL.

API_URL = "https://your-railway-backend.railway.app"
